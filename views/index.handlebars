<h1>Phonso's Organic Burgers!</h1>

<h2>Burgers That were ate!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burger's was So Good!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger to the Menu</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Hungry?</label><br>
    <input type="radio" name="devoured" value="1" checked> Tasty!!<br>
    <input type="radio" name="devoured" value="0"> Haven't Ate
  </div>

  <button type="submit">Add Burger</button>
</form>